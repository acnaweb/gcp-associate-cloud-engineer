# Integração dos mapas gcloud

Copie a pasta `gcloud/` e o workflow `.github/workflows/render-gcloud-plantuml.yml`
para a raiz do repositório `gcp-associate-cloud-engineer`.

Adicione ao README raiz existente, se desejar:

```markdown
## Mapas mentais do gcloud

[Explorar mapas mentais de COMPONENTS e ENTITIES](./gcloud/)
```
